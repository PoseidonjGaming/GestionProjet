{% extends 'base.html.twig' %}

{% block title %}Récapitulation{% endblock %}

{% block body %}
<style>
    .example-wrapper { margin: 1em auto; max-width: 800px; width: 95%; font: 18px/1.5 sans-serif; }
    .example-wrapper code { background: #F5F5F5; padding: 2px 6px; }
</style>

<div class="row">
   <div class="col-md-4">
        <select name="projet" id="projet" required>
            {% for unProjet in projets %} 
                <option value="{{unProjet.nom}}">{{unProjet.nom}}</option>       
            {% endfor %}        
        </select>
    </div>
</div>

<a class="btn btn-primary" href="{{ path('insertP') }}" role="button">Ajouter</a>
<a class="btn btn-primary" href="{{ path('SupprP', {'id': unProjet.id }) }}" role="button">Supprimer</a>
<a class="btn btn-primary" href="{{ path('UpdateP', {'id': unProjet.id }) }}" role="button">Modifier</a>
{% endblock %}