{% extends 'base.html.twig' %}
 
{% block title %}Liste des projets archivés{% endblock %}
 
{% block body %}
<style>
    .example-wrapper { margin: 1em auto; max-width: 800px; width: 95%; font: 18px/1.5 sans-serif; }
    .example-wrapper code { background: #F5F5F5; padding: 2px 6px; }
</style>


<h1>Liste des projets archivés</h1>
<p></p>
<div class="row">
  
  {% set tour = 0 %}
    {% for unProjet in projets %}
      {% if unProjet.archive == true %}
{{block('archive1', "projet/projets.html.twig")}}
 <p><a class="btn btn-secondary" href="{{ path('desarchivage', {'idP': unProjet.id }) }}" >Désarchiver le projet</a></p>
{{block('archive2', "projet/projets.html.twig")}}
  {% endif %}
{% set tour = tour + 1 %}
    {% endfor %}     
</div>
{% endblock %}