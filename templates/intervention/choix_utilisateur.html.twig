{% extends 'base.html.twig' %}

{% block title %}Choix du salarié{% endblock %}

{% block body %}
<style>
    .example-wrapper {   width: 95%; font: 18px/1.5 sans-serif; }
    .example-wrapper code { background: #F5F5F5; padding: 2px 6px; }
</style>

<div class="example-wrapper">
<a href="{{path('Interventions') }}" class="btn btn-secondary">Voir les interventions</a>
  <div class="row">
    {% for UnUser in users %}
      <a href="{{path('choixProjets', {'idU':UnUser.id}) }}">
         <div class="card-body" style="width: 18rem;" style="height: 29rem">                       
            <img class="card-img-top" src="/photo/{{UnUser.photo}}"/>
          </div>
        </a>
    {% endfor %}
  </div>
  
{% endblock %}