{% extends "base.html.twig" %}
{% block title %}Choix du technicien{% endblock %}
{% block body %}
<style>
    .example-wrapper { margin: 1em auto; max-width: 800px; width: 95%; font: 18px/1.5 sans-serif; }
    .example-wrapper code { background: #F5F5F5; padding: 2px 6px; }
    .tache { height:123px;  }
</style>

  <div class="row">
    {% for UnUser in users %}
    
    {% if modif =='planning' %}
     <a href="{{path('observerPlanning', {'idU':UnUser.id}) }}">
    {% else %}
        <a href="{{path('choixTacheV2', {'idU':UnUser.id,'modif': modif, 'idPl': idPl}) }}">
      {% endif %}
          <div class="card-body" style="width: 18rem;" style="height: 29rem">                        
            <img class="card-img-top" src="/photo/{{UnUser.photo}}"/>
          </div>
        </a>
    {% endfor %}

 </div>
 {% endblock %}